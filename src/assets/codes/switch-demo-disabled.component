import { Component } from '@angular/core';

@Component({
  selector: 'nel-demo-switch-disabled',
  template: `
    <nel-switch [(ngModel)]="value1" nelDisabled></nel-switch>
    <nel-switch [(ngModel)]="value2" nelDisabled></nel-switch>
  `,
  styles: [
    `
      :host ::ng-deep .el-switch {
        margin: 20px 20px 20px 0;
      }
    `
  ]
})

export class NelDemoSwitchDisabledComponent {
  value1 = true;
  value2 = false;
}
