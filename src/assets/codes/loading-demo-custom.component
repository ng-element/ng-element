import { Component } from '@angular/core';

@Component({
  selector: 'nel-demo-loading-custom',
  template: `
    <nel-loading [nelText]="'拼命加载中'" [nelSpinner]="'loading'" [nelBackground]="'rgba(0, 0, 0, 0.8)'">
      <div class="table">
        table区域，待开发中...
      </div>
    </nel-loading>
  `,
  styles: [
    `
      .table {
        height: 192px;
      }
    `
  ]
})

export class NelDemoLoadingCustomComponent { }
