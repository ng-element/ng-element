import { Component } from '@angular/core';

@Component({
  selector: 'nel-demo-empty-footer',
  template: `
    <nel-empty [nelFooter]="footerTpl">
      <ng-template #footerTpl>
        底部内容
      </ng-template>
    </nel-empty>
  `
})

export class DemoEmptyFooterComponent { }
