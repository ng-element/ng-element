import { Component } from '@angular/core';

@Component({
  selector: 'nel-demo-color-picker-basic',
  template: `
    <nel-color-picker></nel-color-picker>
  `
})

export class NelDemoColorPickerBasicComponent { }
