import { Component } from '@angular/core';

@Component({
  selector: 'nel-demo-pagination-basic',
  template: `
    <p>When you have few pages</p>
    <nel-pagination [total]="50"></nel-pagination>
    <p>When you have more than 7 pages</p>
    <nel-pagination [total]="1000"></nel-pagination>
  `
})

export class NelDemoPaginationBasicComponent { }
