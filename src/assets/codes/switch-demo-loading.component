import { Component } from '@angular/core';

@Component({
    selector: 'nel-demo-switch-loading',
    template: `
    <nel-switch [(ngModel)]="value1" nelLoading> </nel-switch>
    <nel-switch [(ngModel)]="value2" nelLoading> </nel-switch>
  `,
    styles: [
        `
      :host ::ng-deep .el-switch {
        margin: 20px 20px 20px 0;
      }
    `
    ],
    standalone: false
})

export class NelDemoSwitchLoadingComponent {
  value1 = true;
  value2 = false;
}
