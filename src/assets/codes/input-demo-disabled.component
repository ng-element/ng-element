import { Component } from '@angular/core';

@Component({
    selector: 'nel-demo-input-disabled',
    template: `
    <nel-input [nelPlaceHolder]="'请输入内容'" [(ngModel)]="input" nelDisabled></nel-input>
  `,
    styles: [
        `
    .el-input {
      width: 180px;
    }
    `
    ],
    standalone: false
})

export class NelDemoInputDisabledComponent {
  input = '';
}
