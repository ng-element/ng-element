import { Component } from '@angular/core';

@Component({
    selector: 'nel-demo-checkbox-disabled',
    template: `
    <label nel-checkbox [(ngModel)]="checked1" nelDisabled>备选项1</label>
    <label nel-checkbox [(ngModel)]="checked2" nelDisabled>备选项</label>
  `,
    standalone: false
})

export class NelDemoCheckboxDisabledComponent {
  checked1 = false;
  checked2 = true;
}
