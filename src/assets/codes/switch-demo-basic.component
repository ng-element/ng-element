import { Component } from '@angular/core';

@Component({
  selector: 'nel-demo-switch-basic',
  template: `
    <nel-switch [(ngModel)]="value" [nelActiveColor]="'#13ce66'" [nelInactiveColor]="'#ff4949'"></nel-switch>
  `
})

export class NelDemoSwitchBasicComponent {
  value = true;
}
