import { Component } from '@angular/core';

@Component({
  selector: 'nel-demo-skeleton-rows',
  template: `
    <nel-skeleton [nelRows]="5"></nel-skeleton>
  `
})

export class NelDemoSkeletonRowsComponent { }
