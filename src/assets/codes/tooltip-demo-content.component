import { Component } from '@angular/core';

@Component({
    selector: 'nel-demo-tooltip-content',
    template: `
    <button nel-button nel-tooltip [nelContent]="contentTpl" [nelPlacement]="'top'">Top center</button>
    <ng-template #contentTpl>
      多行信息<br/>第二行信息
    </ng-template>
  `,
    standalone: false
})

export class NelDemoTooltipContentComponent { }
