import { Component } from '@angular/core';

@Component({
    selector: 'nel-demo-input-number-disabled',
    template: `
    <nel-input-number [(ngModel)]="num" nelDisabled></nel-input-number>
  `,
    standalone: false
})

export class NelDemoInputNumberDisabledComponent {
  num = 1;
}
